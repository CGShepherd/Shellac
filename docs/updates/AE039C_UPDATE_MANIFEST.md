# AE-039C

Run `APPLY_AE039C.bat`.

If targeted tests pass:
`build_shellac.bat`
then:
`python -m pytest`

Expected: real A/B unit numbers and physical SOIC-8 pins, no synthetic op-amp
0VA pin, explicit follower feedback, board population still 246.
