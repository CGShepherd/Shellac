# AE-040A

Run:

`APPLY_AE040A.bat`

Then:

`python -m pytest tests/test_production_integrity_audit.py -v`

If green:

`python -m pytest`

Governance/audit-state change only; no electrical or PCB geometry change.
