# AE-022D update

Apply over the current uncommitted AE-022 through AE-022C tree.

Run:
`APPLY_AE022D.bat`

The batch applies the exact geometry fix, installs the regression, prints SCH101
net traces, and runs the complete pytest suite.

If green, run the normal Shellac build/native ERC next.
