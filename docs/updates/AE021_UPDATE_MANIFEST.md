# AE-021 larger-phase update

Create `feature/dr039-full-closure`, extract at repository root, run `python tools/apply_dr039_full_closure.py`, then `python -m pytest` and the normal Shellac build/ERC workflow. If clean, commit with `feat(sch103): fully implement DR-039 post-EQ DC isolation`.
